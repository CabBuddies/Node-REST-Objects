{
    "name": "node-rest-objects",
    "version": "1.0.0",
    "description": "Can log \"hello world\" and \"goodbye world\" to the console!",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc --build",
        "test": "mocha -r ts-node/register src/test/**/*.test.ts --timeout 60000",
        "test-nihal": "npx ts-node ./src/test/nihal.ts",
        "test-sahana": "npx ts-node ./src/test/sahana.ts",
        "git": "npm run build && git add . && git commit -am 'Quick Commit' && git push -u origin master"
    },
    "dependencies": {
        "@types/axios": "^0.14.0",
        "@types/chai": "^4.2.13",
        "axios": "^0.20.0",
        "firebase": "^8.1.1",
        "ts-mocha": "^7.0.0"
    },
    "devDependencies": {
        "@types/expect": "^24.3.0",
        "@types/mocha": "^8.0.3",
        "chai": "^4.2.0",
        "mocha": "^8.1.3",
        "typescript": "^4.0.3"
    }
}
